apply plugin: 'com.github.johnrengelman.shadow'

dependencies {
    implementation project(':art-api')
}

shadowJar {
    classifier = ''
    dependencies {
        include(dependency(':art-api'))
    }
}

tasks.build.dependsOn(shadowJar)